const client_port = 3030;
const client_url = 'http://localhost';
const full_url = `${client_url}:${client_port}`

const server_port = 5000;


const db_config = {
    user:'jz75',
    host:'jz75.teaching.cs.st-andrews.ac.uk',
    database:'jz75_CS4203_D2',
    password:'.8e96x3iPGU6Q9',
}

module.exports = {client_port, client_url, full_url, db_config}